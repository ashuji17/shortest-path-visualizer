# shortest-path-visualizer
Developed an interactive maze solver using Breadth-First Search (BFS) to compute the shortest path in a grid-based environment.
<br>
Implemented real-time pathfinding visualization using the curses library, dynamically rendering traversal and explored nodes.
